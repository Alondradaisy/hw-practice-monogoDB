const recipe = require('../model/recipe');
const bcrypt = require('bcrypt');

const mongoose = require('mongoose');

const Recipe = new mongoose.recipe({
    RecipeName: {
        type: String
    },
});

